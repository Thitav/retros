diff --git a/config.sub b/config.sub
index 38f3d037a78..596ef10a1ba 100755
--- a/config.sub
+++ b/config.sub
@@ -1746,7 +1746,7 @@ case $os in
 	     | scout* | superux* | sysv* | rtmk* | tpf* | windiss* \
 	     | powermax* | dnix* | nx6 | nx7 | sei* | dragonfly* \
 	     | skyos* | haiku* | rdos* | toppers* | drops* | es* \
-	     | onefs* | tirtos* | phoenix* | fuchsia* | redox* | bme* \
+	     | onefs* | tirtos* | phoenix* | fuchsia* | redox* | retros* | bme* \
 	     | midnightbsd* | amdhsa* | unleashed* | emscripten* | wasi* \
 	     | nsk* | powerunix* | genode* | zvmoe* | qnx* | emx* | zephyr* \
 	     | fiwix* )
diff --git a/fixincludes/mkfixinc.sh b/fixincludes/mkfixinc.sh
index 7112f4dcd64..5fcd04fe827 100755
--- a/fixincludes/mkfixinc.sh
+++ b/fixincludes/mkfixinc.sh
@@ -11,6 +11,8 @@ target=fixinc.sh
 
 # Check for special fix rules for particular targets
 case $machine in
+    *-retros* | \
+    *-*-retros* | \
     i?86-*-cygwin* | \
     *-mingw32* | \
     powerpc-*-eabisim* | \
diff --git a/gcc/config.gcc b/gcc/config.gcc
index 8fe99616f82..f0a8724422f 100644
--- a/gcc/config.gcc
+++ b/gcc/config.gcc
@@ -776,6 +776,13 @@ x86_cpus="generic intel"
 
 # Common parts for widely ported systems.
 case ${target} in
+*-*-retros*)
+  extra_options="$extra_options gnu-user.opt"
+  gas=yes
+  gnu_ld=yes
+  default_use_cxa_atexit=yes
+  use_gcc_stdint=provide
+  ;;
 *-*-darwin*)
   tmake_file="t-darwin "
   tm_file="${tm_file} darwin.h"
@@ -1236,6 +1243,12 @@ case ${target} in
 esac
 
 case ${target} in
+i[34567]86-*-retros*)
+    tm_file="${tm_file} i386/unix.h i386/att.h elfos.h glibc-stdint.h i386/i386elf.h retros.h gnu-user.h"
+    ;;
+x86_64-*-retros*)
+    tm_file="${tm_file} i386/unix.h i386/att.h elfos.h glibc-stdint.h i386/i386elf.h i386/x86-64.h retros.h gnu-user.h"
+    ;;
 aarch64*-*-elf | aarch64*-*-fuchsia* | aarch64*-*-rtems*)
 	tm_file="${tm_file} elfos.h newlib-stdint.h"
 	tm_file="${tm_file} aarch64/aarch64-elf.h aarch64/aarch64-errata.h aarch64/aarch64-elf-raw.h"
diff --git a/libgcc/config.host b/libgcc/config.host
index ac10ccc4340..91823b310e6 100644
--- a/libgcc/config.host
+++ b/libgcc/config.host
@@ -389,6 +389,14 @@ i[34567]86-*-cygwin* | x86_64-*-cygwin*)
 esac
 
 case ${host} in
+i[34567]86-*-retros*)
+	extra_parts="$extra_parts crti.o crtbegin.o crtend.o crtn.o"
+	tmake_file="$tmake_file i386/t-crtstuff t-crtstuff-pic t-libgcc-pic"
+	;;
+x86_64-*-retros*)
+	extra_parts="$extra_parts crti.o crtbegin.o crtend.o crtn.o"
+	tmake_file="$tmake_file i386/t-crtstuff t-crtstuff-pic t-libgcc-pic"
+	;;
 aarch64*-*-elf | aarch64*-*-rtems*)
 	extra_parts="$extra_parts crtbegin.o crtend.o crti.o crtn.o"
 	extra_parts="$extra_parts crtfastmath.o"
diff --git a/libstdc++-v3/crossconfig.m4 b/libstdc++-v3/crossconfig.m4
index b3269cb88e0..9bbf21412eb 100644
--- a/libstdc++-v3/crossconfig.m4
+++ b/libstdc++-v3/crossconfig.m4
@@ -5,6 +5,13 @@ dnl
 AC_DEFUN([GLIBCXX_CROSSCONFIG],[
 # Base decisions on target environment.
 case "${host}" in
+  *-retros*)
+    GLIBCXX_CHECK_COMPILER_FEATURES
+    GLIBCXX_CHECK_LINKER_FEATURES
+    GLIBCXX_CHECK_MATH_SUPPORT
+    GLIBCXX_CHECK_STDLIB_SUPPORT
+    ;;
+
   arm*-*-symbianelf*)
     # This is a freestanding configuration; there is nothing to do here.
     ;;
new file mode 100644
index 00000000000..59e7df71afc
--- /dev/null
+++ b/gcc/config/retros.h
@@ -0,0 +1,28 @@
+ /* Useful if you wish to make target-specific GCC changes. */
+ #undef TARGET_RETROS
+ #define TARGET_RETROS 1
+ 
+ /* Default arguments you want when running your
+    i686-retros-gcc/x86_64-retros-gcc toolchain */
+ #undef LIB_SPEC
+ #define LIB_SPEC "-lc" /* link against C standard library */
+ 
+ /* Files that are linked before user code.
+    The %s tells GCC to look for these files in the library directory. */
+ #undef STARTFILE_SPEC
+ #define STARTFILE_SPEC "crt0.o%s crti.o%s crtbegin.o%s"
+ 
+ /* Files that are linked after user code. */
+ #undef ENDFILE_SPEC
+ #define ENDFILE_SPEC "crtend.o%s crtn.o%s"
+ 
+ /* Additional predefined macros. */
+ #undef TARGET_OS_CPP_BUILTINS
+ #define TARGET_OS_CPP_BUILTINS()      \
+   do {                                \
+     builtin_define ("__retros__");      \
+     builtin_define ("__unix__");      \
+     builtin_assert ("system=retros");   \
+     builtin_assert ("system=unix");   \
+     builtin_assert ("system=posix");   \
+   } while(0);
